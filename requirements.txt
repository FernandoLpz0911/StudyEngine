# Core engine — generators, FSRS, recall, analytics, CLI, HTTP API, tests.
# Knowledge-tracing (DKT) is an optional extra: see requirements-dkt.txt.
fsrs>=4.0.0
numpy>=1.26
scipy>=1.11
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
pytest>=8.0
ruff>=0.6
